# GuitarProVST3 AI 协作规范

本文件是供 AI/自动化协作使用的唯一协作规范和文档入口。开始任务前先阅读本文件；开发目标、实施计划、实现状态和能力证据分别维护在下方链接的文档中。

## 协作规范

- 默认使用中文沟通和编写文档。代码、命令、路径、API、协议字段、状态值和原始界面文字保留原样。
- 用户明确要求执行修改、清理、验证或提交时，直接完成工作并报告结果；不要只停留在计划或建议。遇到阻塞时说明具体原因、已完成内容和下一步需要的外部条件。
- 开始编辑前检查 `git status` 和相关文件。保留用户已有改动，不回滚无关文件，不覆盖未授权的外部配置或数据。
- 遵循“不要过度设计，能不做就不做”。优先复用现有 C++/Qt、宿主原生接口、PowerShell 脚本和本地文档结构；只有在解决实际问题时才增加抽象、依赖或流程。
- 每项行为都区分“已实现、已验证、实验性、未实现、宿主受限”。没有实际状态、文件结果或回归证据，不得把编译成功、返回 JSON、菜单可枚举或 DLL 加载成功写成能力已完成。
- 修改后运行与风险匹配的检查，至少执行语法/格式检查和受影响的专项验证。完整原生回归需要真实 Guitar Pro 宿主时，明确说明未运行及原因。
- 提交前检查 `git diff --check`、`git status` 和敏感文件；不要提交令牌、客户端配置、临时宿主、缓存、构建产物、安装包或 `artifacts/` 内容。提交信息说明实际结果，不夸大验收范围。
- 协作规范只在本文件维护；开发目标和实施计划见文档入口中的计划文档。README、安装说明、覆盖清单和原生开发说明通过链接引用，不复制一套新的规则。

## 文档入口

- [实时实现计划](docs/REALTIME_IMPLEMENTATION_PLAN.md)：开发目标、交付边界、P0–P6 阶段、验收条件、核心约束和里程碑。
- [P0 实现记录](docs/P0_IMPLEMENTATION.md)：当前实现边界、构建方式和隔离宿主验证入口。
- [VST3 效果器链路预研报告（最终版）](gp8-vst3-research-20260909/VST3效果器链路预研报告_最终版.md)：接入点、私有 ABI、`IAudioBuffer`、RSE 处理链和输入输出限制的依据。
- [原始预研报告](gp8-vst3-research-20260909/research_report.md)：预研原始记录和补充证据。
